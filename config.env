# Audiobooks Project - Development Configuration
# ================================================
# This configuration is for PROJECT DEVELOPMENT ONLY.
# All paths point to test data WITHIN the project directory.
#
# CRITICAL: This project is COMPLETELY SEPARATE from the installed application.
# The installed application lives at:
#   - /raid0/Audiobooks (data)
#   - /opt/audiobooks (code)
#   - /etc/audiobooks (config)
#
# There must be NO cross-references between project and application.

# =============================================================================
# PROJECT ROOT
# =============================================================================
PROJECT_DIR="/raid0/ClaudeCodeProjects/Audiobook-Manager"

# =============================================================================
# DEVELOPMENT DATA PATHS (all within project - no external dependencies)
# =============================================================================

# Test audiobook library (synthetic test data)
AUDIOBOOK_DIR="${PROJECT_DIR}/library/testdata"

# Test data paths
AUDIOBOOKS_DATA="${PROJECT_DIR}/library/testdata"
AUDIOBOOKS_LIBRARY="${PROJECT_DIR}/library/testdata/Library"
AUDIOBOOKS_SOURCES="${PROJECT_DIR}/library/testdata/Sources"
AUDIOBOOKS_SUPPLEMENTS="${PROJECT_DIR}/library/testdata/Supplements"
AUDIOBOOKS_LOGS="${PROJECT_DIR}/library/testdata/logs"

# Development database (small, synthetic data only)
DATABASE_PATH="${PROJECT_DIR}/library/backend/audiobooks-dev.db"
AUDIOBOOKS_DATABASE="${PROJECT_DIR}/library/backend/audiobooks-dev.db"

# Development covers
COVER_DIR="${PROJECT_DIR}/library/web-v2/covers"
AUDIOBOOKS_COVERS="${PROJECT_DIR}/library/web-v2/covers"

# JSON data output (test data)
DATA_DIR="${PROJECT_DIR}/library/data"

# =============================================================================
# DEVELOPMENT SERVER PORTS (different from production)
# =============================================================================

# Web interface port (dev: 9090, prod: 8443)
WEB_PORT=9090

# API server port (dev: 6001, prod: 5001)
API_PORT=6001

# HTTPS ports for development
AUDIOBOOKS_WEB_PORT=9443
AUDIOBOOKS_API_PORT=6001
AUDIOBOOKS_HTTP_REDIRECT_PORT=9081

# =============================================================================
# DEVELOPMENT FLAGS
# =============================================================================
AUDIOBOOKS_DEV_MODE="true"
AUDIOBOOKS_HTTPS_ENABLED="false"
AUDIOBOOKS_HTTP_REDIRECT_ENABLED="false"

# =============================================================================
# LEGACY COMPATIBILITY (mapped to dev paths)
# =============================================================================
SOURCES_DIR="${AUDIOBOOK_DIR}/Sources"
OPUS_DIR="${AUDIOBOOK_DIR}/Library"
CONVERTED_DIR="${AUDIOBOOK_DIR}/Library"
