# =============================================================================
# Docker Build Exclusions
# =============================================================================
# This file excludes files not needed in the Docker container

# Git
.git
.gitignore
.github

# Python virtual environments and caches
__pycache__
*.py[cod]
*.so
venv/
.venv/
ENV/
*.egg-info/
library/venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Database (will be mounted as volume or auto-created)
*.db
library/backend/*.db

# Covers (will be mounted as volume)
library/web/covers/
library/web-v2/covers/

# Development files
*.log
*.tmp
*.temp

# Sensitive files
.env
.env.local
*.pem
*.key
authcode.txt
*.authcode
credentials.json
secrets/
config.env

# =============================================================================
# Native Installation Files (NOT needed in Docker)
# =============================================================================
# These are for native Linux/macOS installs, Docker uses docker-entrypoint.sh

# Install scripts
install.sh
install-user.sh
install-system.sh
install-services.sh
launch.sh

# Shell config loader (not used in Docker)
lib/

# Config templates (Docker uses environment variables)
etc/

# Security documentation (not needed in container)
SECURITY.md

# Legacy web interface (using web-v2 only)
library/web/

# Certs directory (Docker generates/mounts its own)
library/certs/
certs/
