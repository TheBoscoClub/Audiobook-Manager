#!/bin/bash
# Vox Grotto Upgrade Script
# Wrapper — delegates to the full upgrade.sh in the scripts directory
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
exec "${SCRIPT_DIR}/upgrade.sh" --target "$(dirname "${SCRIPT_DIR}")" "$@"
