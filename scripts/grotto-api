#!/bin/bash
# Vox Grotto Library API Server
# Wrapper script — sources config and launches the Flask API
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
source "${SCRIPT_DIR}/../lib/grotto-config.sh"
cd "${AUDIOBOOKS_HOME}/library/backend"
exec "$(grotto_python)" api_server.py "$@"
