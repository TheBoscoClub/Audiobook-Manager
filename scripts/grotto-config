#!/bin/bash
# Audiobook Library Configuration Viewer
# Wrapper script — sources config and prints current settings
SCRIPT_DIR="$(dirname "$(readlink -f "$0")")"
source "${SCRIPT_DIR}/../lib/audiobook-config.sh"
audiobooks_print_config "$@"
