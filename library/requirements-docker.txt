# Docker-specific requirements
# The Docker container is a standalone, self-contained product for portability.
# It does NOT include Audible CLI integration — use the native install for that.

Flask>=3.1.3             # GHSA-68rp-wp8r-4726
mutagen>=1.48.1
gunicorn>=26.2.0
gevent>=26.8.0
gevent-websocket>=0.10.1
flask-sock>=0.7.0
croniter>=6.2.4
requests>=2.34.2
cryptography>=50.0.0     # CVE-2026-39892 (47.0.0 was floor); 50.0.0 latest stable 2026-08
webauthn>=3.0.0,<3.1.0   # 3.0 requires cbor2>=6.1.2 — compatible with cbor2>=6.1.4 (CVE-2026-26209); the pre-3.0 cbor2<6.0.0 cap is gone
pyotp>=2.10.0
defusedxml>=0.7.1
qrcode[pil]>=8.2
sqlcipher3>=0.6.2
# Security: Pin minimum versions for transitive deps with CVEs
urllib3>=2.7.0      # CVE-2026-21441, CVE-2026-44431, CVE-2026-44432
pyasn1>=0.6.4       # CVE-2026-23490, CVE-2026-30922
filelock>=3.32.4    # CVE-2025-68146
marshmallow>=4.3.1  # CVE-2025-68480 (actual installed: 4.x)
pynacl>=1.6.2       # CVE-2025-69277
werkzeug>=3.1.8     # CVE-2025-66221, CVE-2026-21860, GHSA-29vq-49wr-vm6x
pillow>=12.3.0      # GHSA-cfh3-3jmp-rvhc (OOB write on PSD)
pyopenssl>=26.4.0   # CVE-2026-27448, CVE-2026-27459 (26.2.0 supports cryptography 48)
cbor2>=6.1.4        # CVE-2026-26209 (DoS via CBOR recursion, used by webauthn)
idna>=3.19          # CVE floor; transitive via requests/cryptography
edge-tts>=7.2.8     # streaming TTS worker shells out to python -m edge_tts
pypinyin>=0.55.0    # search_cjk uses pypinyin for Chinese character romanization

# ── Transitive security pins ──────────────────────────────────────────────
# Pulled in by edge-tts / CacheControl, not used directly. Pinned because the
# versions those packages would otherwise resolve carry live advisories:
#   aiohttp <3.14.3 — PYSEC-2026-2104 (arbitrary code execution via
#     CookieJar.load on untrusted input), PYSEC-2026-3546 (request smuggling
#     via the WebSocket upgrade path), PYSEC-2026-3545 (out-of-bounds heap
#     read in the C response parser), plus 8 further DoS/cookie-scope issues.
#   msgpack <1.2.1 — PYSEC-2026-3625 (out-of-bounds read / SEGV when an
#     Unpacker is reused after a caught error).
# Both are within their parents' declared ranges (edge-tts: aiohttp<4.0.0;
# CacheControl: msgpack<2.0.0), so this constrains without conflicting.
aiohttp>=3.14.3
msgpack>=1.2.1
