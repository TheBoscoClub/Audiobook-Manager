# Development / CI dependencies — not installed into production venv.
# Install with: pip install -r library/requirements-dev.txt
-r requirements.txt

# Test framework
pytest>=9.0.3
pytest-cov>=7.1.0
pytest-asyncio>=1.4.0
pytest-playwright>=0.8.0
requests-mock>=1.12.1

# Linters / formatters
ruff>=0.15.17
mypy>=2.1.0
black>=26.5.1
isort>=8.0.1

# Security scanners
bandit>=1.9.4
pip-audit>=2.10.0

# Transitive dev dependencies — pinned to current versions to ensure
# dev environments stay reproducible across fresh installs. These are
# pulled in by the tools above; bumping the floor keeps us on the
# current-major line and avoids CVE-tracked older releases.
rich>=15.0.0
greenlet>=3.5.1
librt>=0.11.0
# radon 6.0.1 requires mando>=0.6,<0.8; stay on the 0.7.x line (latest
# pre-0.8 release is 0.7.1) so `pip check` stays clean.
mando>=0.8.2,<0.9
# astroid is pylint's runtime AST engine. pylint 4.0.5 (latest stable)
# constrains astroid to `<=4.1.dev0,>=4.0.2`; 4.1.2 is compatible.
# Re-visit upper bound when pylint 4.1+ releases upstream.
astroid>=4.1.2,<4.2
packaging>=26.2
platformdirs>=4.10.0
types-croniter>=6.2.2.20260518
types-requests>=2.33.0.20260518
zope.interface>=8.4
