# Development / CI dependencies — not installed into production venv.
# Install with: pip install -r library/requirements-dev.txt
-r requirements.txt

# Test framework
pytest>=9.1.1
pytest-cov>=7.1.0
pytest-asyncio>=1.4.0
pytest-playwright>=0.9.0
requests-mock>=1.12.1

# Linters / formatters
# ruff is the canonical formatter AND import sorter (`ruff format`, plus the
# `I` rules in `ruff check`) — see the rationale header in pyproject.toml.
# black and isort are deliberately absent: neither was invoked by CI, by the
# /test audit, or by any script, and running black would reformat 134 files
# straight through the `ruff format --check` gate.
ruff>=0.16.2
mypy>=2.3.0

# Security scanners
bandit>=1.9.4
pip-audit>=2.10.1

# Transitive dev dependencies — pinned to current versions to ensure
# dev environments stay reproducible across fresh installs. These are
# pulled in by the tools above; bumping the floor keeps us on the
# current-major line and avoids CVE-tracked older releases.
rich>=15.0.0
greenlet>=3.5.5
librt>=0.15.0
# radon is the cyclomatic-complexity scanner used by the /test audit's code
# quality phase. It was previously an undeclared transitive assumption — only
# its own dependency (mando) was pinned here, so a fresh dev environment had
# the constraint without the tool it exists to constrain.
radon>=6.0.1
# radon 6.0.1 requires mando>=0.6,<0.8; stay on the 0.7.x line (latest
# pre-0.8 release is 0.7.1) so `pip check` stays clean. The floor previously
# read `>=0.8.2,<0.9`, which contradicted this comment and is outside radon's
# supported range.
mando>=0.8.2,<0.9
# astroid is NOT pinned here, deliberately. It is pylint's AST engine and
# nothing else in this file depends on it; pylint itself was never declared as
# a dev dependency (it is not a CI gate — ruff and mypy are), so the pin
# constrained a package that had no reason to be installed. It was also
# unsatisfiable: the pin read `astroid>=4.1.2,<4.2` with a comment claiming
# pylint 4.0.5 accepts 4.1.2, but every current pylint (through 4.0.6) caps
# astroid at `<=4.1.dev0` — installing both resolved pylint backwards to 2.3.0.
# `[tool.pylint.typecheck]` in pyproject.toml is retained for ad-hoc runs; if
# pylint is ever made a gate, declare pylint here and let it resolve astroid.
packaging>=26.3
platformdirs>=4.11.2
types-croniter>=6.2.4.20260711
types-requests>=2.33.0.20260712
zope.interface>=8.5
